import { createTheme } from '@material-ui/core/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#dd4587',
    },
    secondary: {
      main: '#ff8941',
    },
    inherit: {
      main: '#ff0000',
    },
    success: {
      main: '#00ff00',
    }
  },
});